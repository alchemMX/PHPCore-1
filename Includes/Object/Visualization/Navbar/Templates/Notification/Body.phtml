
<?php if ($body = $this->build->user->notification($row['data'])) : ?>
    <?= $this->build->user->image($row['data']) ?>
    <div class="inner">
        <?= $body ?>
        <br>
        <time><?= $this->build->date->long($row['data']['user_notification_time'], true) ?></time>
    </div>
<?php else : ?>
    <div>
        <?= $L_NAVBAR['L_NOTIFICATION_EMPTY'] ?>
    </div>
<?php endif; ?>